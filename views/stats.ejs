<!-- views/stats.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stats</title>
</head>
<body>
    <h1>Live Stats</h1>
    <ul id="statsList"></ul>

    <script src="https://unpkg.com/primus/dist/primus.js"></script>
    <script>
        const primus = new Primus();

        primus.on('data', (data) => {
            updateStatsList(data);
        });

        function updateStatsList(data) {
            const statsList = document.getElementById('statsList');
            const listItem = document.createElement('li');
            listItem.textContent = `${data.team}: ${data.score}`;
            statsList.appendChild(listItem);
        }
    </script>
</body>
</html>
